# Main Imports
disnake           # A modern, Pythonic, asynchronous library for Discord API interaction.

# Environment 
python-dotenv     # Reads settings from .env files and sets them as environment variables.

# HTTP Requests
requests          # Python HTTP library for making requests and handling responses.

# Database
psycopg2-binary   # PostgreSQL adapter for Python.
databases[sqlite] # Async database support for Python using the `database` library with SQLite extension.

# Migrations
alembic           # A database migrations tool for SQLAlchemy.

# Async Tasks
celery            # Distributed task queue for asynchronous tasks.

# Media Downloading
youtube-dl        # Command-line program to download videos from YouTube and other sites.

# Images  
Pillow            # Python Imaging Library (PIL), which adds support for opening, manipulating, and saving image files.

# Web API
fastapi           # A modern, fast, web framework for building APIs with Python 3.7+.

# Documentation
mkdocs            # A static site generator that builds HTML documentation from markdown.
sphinx            # Python documentation generator.

# Testing
pytest            # A testing framework for Python.
pytest-asyncio     # Pytest plugin for testing asyncio code.
pytest-mock       # Pytest plugin for mocking and spying.

# Code Quality
pylint            # A static code analysis tool for finding errors in Python code.
flake8            # A tool for style guide enforcement and linting in Python.

# Deploys
docker            # Platform for developing, shipping, and running applications using containers.
ansible           # An automation tool for managing configuration and deployments.

# Monitoring
prometheus-client # Python client for the Prometheus monitoring system.
grafana-api       # Python bindings for the Grafana HTTP API.

# Debugging
ipdb              # IPython debugger, an enhanced alternative to the built-in pdb debugger.

# Async
anyio             # Asynchronous concurrency framework.

# API Clients
httpx             # A fully featured HTTP client for Python 3, which provides sync and async APIs.
ratelimit         # A simple Python rate-limiting library.

# Notifications
pynotifier        # Python package for sending cross-platform desktop notifications.

# Authentication
pyjwt             # Python library for encoding and decoding JSON Web Tokens (JWT).
argon2-cffi       # Python bindings for the Argon2 password hashing algorithm.

